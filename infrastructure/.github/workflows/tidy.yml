name: Auto Tidy Go Modules
on:
  push:
    branches: [ main ]
jobs:
  tidy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run Tidy
        run: |
          cd apps/go-gateway
          go mod tidy
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: automated go mod tidy execution"
          file_pattern: "apps/go-gateway/go.mod apps/go-gateway/go.sum"
